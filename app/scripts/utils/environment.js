/**
 * Application Environment Constants
 */

define([],function(){
    var constants = {
        TICKER_ENABLED : true,
        TICKER_DISABLED: false,
        DEFAULT_VALUE : 0
    }

    return constants;
});
